feat: mobile app improvements - bugfixes, UX, performance and new features

Bug fixes:
- Fix auth: tabs layout redirects to login when no user (was setting ok status)
- Fix RPC: only explicit true grants trainer access (null/error no longer bypasses)
- Fix home: setLoading(false) on early exit; chart queries now fetch latest records (was oldest)
- Fix chat: cleanup Realtime channel on unmount; re-init when clientData arrives late
- Fix chat: failed send removes optimistic message and restores input for retry
- Fix training: stale closure in draft-save useEffect (add all guards to dep array)
- Fix training: error handling and Alert for workout_logs update failures
- Fix training: tabPress only resets workout when screen is already focused
- Fix nutrition: upsertLog returns boolean; toggleMeal rolls back optimistic state on DB error
- Fix nutrition: parseFloat instead of parseInt for macro values
- Fix metrics: pass group prop to ParamChart so x-axis labels match selected grouping
- Fix checkin: draft buttons moved to daily section, weekly reverts to single button
- Fix workout history: unique keys in VolumeChart SVG elements (duplicate key error)
- Fix home: useEffect depends on clientData.clientId to re-run when context arrives late

Performance:
- Metrics: add .limit(365) to unbounded checkins and daily_logs fetches
- Home chart: fetch newest 20 records descending then reverse for chronological display

Features:
- Training: RestTimer persists across tab switches (endsAt timestamp + AppState listener)
- Training: real-time draft save on exerciseLogs change (not only on blur)
- Training: edit completed workouts from current week (pre-fills saved values)
- Training: isScreenFocused ref prevents workout reset when returning from another tab
- Nutrition: real-time macro draft save on every change (not only on blur)
- Check-in: daily section gets Spremi (draft) + Posalji (DB) two-button layout
- Auth: forgot password and set password screens with deep link handling
- Push notifications: send-client-push Edge Function for messages, comments, packages
- ClientContext: centralize client/trainer ID fetching across all screens
